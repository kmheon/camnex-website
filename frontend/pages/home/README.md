# Home page

Standalone home-page entry point.

- `index.html` contains the page markup.
- `home.css` contains page-specific styles.
- `home.js` contains lightweight page behavior.

Open `index.html` directly in a browser to preview the page.
